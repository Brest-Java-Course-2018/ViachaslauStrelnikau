import { MDirective } from './m.directive';

describe('MDirective', () => {
  it('should create an instance', () => {
    const directive = new MDirective();
    expect(directive).toBeTruthy();
  });
});
